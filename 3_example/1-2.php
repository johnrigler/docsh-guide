<div class=spacing>
  <div>

Above you will see that I ran the 'pre' command from the command line and then entered into a subshell.  In that shell I typed the unix command 'hostname' and the machine returned a value to me.  This is accomplished with the 'script' command.
  </div>
</div>

